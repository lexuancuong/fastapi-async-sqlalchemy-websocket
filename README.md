# SIMPLE VERSION, JUST FOR CONSULTATION

Docker, Async SQLAlchemy, Fastapi, Websocket(of Starlette)

Run this project by:

    docker-compose up

Remind: Need to add .env file inside `./app` before running this command.